server:
  port: 8671
spring:
  application:
    name: discover-server
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    service-url:
      defaultZone: http://localhost:8671/eureka
